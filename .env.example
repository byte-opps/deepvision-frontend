# Copy this file to `.env` and adjust as needed.
# Never commit your real `.env` to git.

# Where the frontend sends API requests.
# Backend runs on this server at the public IP above.
VITE_API_URL=http://193.237.154.180:8076

# For production behind HTTPS / a domain, e.g.:
# VITE_API_URL=https://deepvision.yourdomain.com
